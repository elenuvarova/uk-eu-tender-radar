# Leave DATABASE_URL blank locally → the backend uses a built-in SQLite file.
# On Render it is injected automatically from the Postgres database → Postgres.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

PORT=3001
NODE_ENV=development
